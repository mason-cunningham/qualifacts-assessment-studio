# Public build settings used by Netlify (and `npm run build`).
# Everything here is safe to commit: the publishable key is designed for browsers.
# NEVER put the service-role / secret key in any .env file.
VITE_SUPABASE_URL=https://thjclunkjqnknsozlyyj.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_TIbTa091cg_sQe8PU4WIYQ_2WBFUmqg

# The one public site. Update if the Netlify site gets a different name,
# and switch to https://assess.qualifacts.com once DNS exists.
VITE_PUBLIC_BASE_URL=https://qualifacts-assess.netlify.app
